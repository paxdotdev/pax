[workspace]

members = [
    "pax",
    "pax-core",
    "pax-chassis-web",
    "pax-macro-expression",
    "pax-compiler",
    "pax-dev-server",
    "pax-properties-coproduct",
]


[profile.release]
lto = true
opt-level = 'z'