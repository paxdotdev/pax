
{{original_tokens}}

#[cfg(feature = "parser")]
impl pax_compiler_api::PathQualifiable for {{pascal_identifier}} {
    fn get_fully_qualified_path(atomic_self_type: &str) -> String {
        module_path!().to_owned() + "::" + atomic_self_type
    }
}