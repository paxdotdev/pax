
for (v, i) in self.vec {
    <Rectangle x={Size::Pixels(v)} width=100px fill={Fill::Solid(Color::rgb(0.5, 0.5, 1.0 / (10.0 - 0.02*v)))} />
}

@handlers{
    mount: handle_mount
	pre_render: handle_pre_render
}

