<Group @click=self.toggle>
    if self.on {
        <Rectangle fill=color stroke=BLACK />
    }
    if !self.on {
        <Rectangle fill=BLACK stroke=WHITE />
    }
</Group>

@settings {
    @mount: mount,
}