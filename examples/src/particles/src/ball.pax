<Ellipse class=ball />

@settings {
    @mount: handle_mount,
    @tick: handle_tick,

    .ball {
        anchor_x:50%
        anchor_y:50%
        fill: { hsla(hue, 85%, 50%, 55%) }
        stroke: WHITE,
        width: { (diameter)px }
        height: { (diameter)px }
    }
}
