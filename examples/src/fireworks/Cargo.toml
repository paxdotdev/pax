[package]
name = "fireworks"
version = "0.15.4"
edition = "2021"
default-run = "run"

[dependencies]
pax-engine = { version = "0.15.5", path="../../../pax-engine" }
pax-std = { version = "0.15.5", path="../../../pax-std"}
serde_json = {version = "1.0.95", optional = true}

[lib]
crate-type = ["cdylib", "rlib"]

[[bin]]
name = "parser"
path = "src/lib.rs"
required-features = ["parser"]

[[bin]]
name = "run"
path = "bin/run.rs"

[features]
parser = ["pax-std/parser", "pax-engine/parser", "dep:serde_json"]
web = ["pax-engine/web"]

