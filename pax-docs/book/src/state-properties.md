# State & Properties
<!-- summary: Stateful components and lifecycle hooks. -->
<!-- tags: state, properties -->

- `Property<T>` fields and the reactive state model.
- Lifecycle hooks: `@mount`, `@tick`, `@pre_render`.
