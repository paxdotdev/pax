
for (rect, i) in self.rects {
    <Rectangle
        width={rect.width}
        height={rect.height}
        transform={Transform2D::translate(rect.x, rect.y)}
    />
}

@events {
    did_mount: handle_did_mount
}